apiVersion: v1
kind: Service
metadata:
  name: {{ include "subscription-service.matchbox.fullname" . }}
  labels:
    {{- include "subscription-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: matchbox
spec:
  type: {{ .Values.matchbox.service.type }}
  ports:
    - name: http
      port: {{ .Values.matchbox.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "subscription-service.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: matchbox
