# e2e/realstack/docker-compose.yml
#
# H1 RealStackHarness — the docker-compose stack for the production
# fhir-subs binary's e2e tests. Every service is a real-software
# dependency the binary actually talks to in production. No service is
# a Go-language fake.
#
# OP #344 (Realstack simplification A) deleted the prometheus,
# otel-collector, coredns, mitmproxy, nginx, and test-token-mint
# services. Their assertions live in proper unit/integration tests now:
#   - /metrics surfacing named counters: cmd/fhir-subs/wiring_observability_test.go
#   - tracing pipeline span emission: internal/api/handlers/tracing_test.go +
#     internal/infra/observability/tracing/tracing_test.go (in-memory exporter)
#   - SSRF / DNS-rebinding / IDN: internal/api/handlers/url_validator_test.go
#     (against the existing handlers.Resolver seam)
#   - STARTTLS-strip: internal/channel/email/email_test.go
#     (against the in-process testrelay that can opt out of advertising STARTTLS)
# See e2e/realstack/doc.go for the full simplification rationale.
#
# Pinned image versions are deliberate: the harness binds the test
# matrix to a single reproducible upstream. Bump pins via a separate MR
# so a stack regression is bisectable.
#
# Each test boots its own copy of this stack with a unique compose
# project name (passed via `docker compose -p <project>`). Per-test
# project namespaces give isolated containers, networks, and volumes
# even when 50+ tests run concurrently.
#
# All services bind on 0 (random ephemeral host port) — Boot reads the
# rendered ports back via `docker compose port` so two concurrent stacks
# never collide on a host port. The compose file uses ${...} env-var
# substitution for image pins so the harness can override versions per
# test if a future story needs a multi-version matrix.

services:
  # PostgreSQL 16 — the database. migrate.Up runs against this.
  postgres:
    image: ${REALSTACK_POSTGRES_IMAGE:-postgres:16-alpine}
    environment:
      POSTGRES_USER: fhirsubs
      POSTGRES_PASSWORD: fhirsubs
      POSTGRES_DB: fhirsubs
    ports:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fhirsubs -d fhirsubs"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  # Keycloak 24 — real OIDC issuer. Exposes /realms/fhir-subs/protocol/
  # openid-connect/{token,certs}. Replaces every in-process
  # principalMiddleware and Bearer-test cheat in the legacy harness.
  #
  # `start-dev` is intentional: the test stack runs in dev mode for
  # speed (no production cluster bootstrap). The realm is provisioned
  # by the harness via Keycloak's REST API after the container reports
  # ready (see boot.go).
  keycloak:
    image: ${REALSTACK_KEYCLOAK_IMAGE:-quay.io/keycloak/keycloak:24.0}
    command: ["start-dev", "--http-port=8080"]
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HEALTH_ENABLED: "true"
      KC_METRICS_ENABLED: "true"
    ports:
      - "8080"
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/8080 && printf 'GET /realms/master/.well-known/openid-configuration HTTP/1.0\\r\\nHost: localhost\\r\\n\\r\\n' >&3 && grep -q '200 OK' <&3 || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 30s

  # HAPI FHIR JPA Server — real R4/R4B/R5 reference FHIR server. The
  # FHIR scan runner and vendor change-feed runner point at this for
  # real Bundle hydration and resource lookup.
  hapi-fhir:
    image: ${REALSTACK_HAPI_IMAGE:-hapiproject/hapi:latest}
    environment:
      hapi.fhir.fhir_version: R4
      hapi.fhir.allow_external_references: "true"
      hapi.fhir.cors.allow_credentials: "false"
      hapi.fhir.openapi_enabled: "false"
      spring.main.allow-bean-definition-overriding: "true"
    ports:
      - "8080"
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/8080 && printf 'GET /fhir/metadata HTTP/1.0\\r\\nHost: localhost\\r\\n\\r\\n' >&3 && grep -q '200 OK' <&3 || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 30s

  # Mailpit — real SMTP relay with REST API for captured-message
  # assertions. Replaces every in-process SMTP fake in the email
  # channel test path.
  mailpit:
    image: ${REALSTACK_MAILPIT_IMAGE:-axllent/mailpit:latest}
    ports:
      - "1025"   # SMTP
      - "8025"   # REST API + UI
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8025/api/v1/info >/dev/null || exit 1"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  # OP #344: prometheus, otel-collector, coredns, nginx, mitmproxy
  # services were deleted from this compose file. Their assertions are
  # exercised by unit/integration tests against the production binary's
  # in-process seams (see header comment for pointers).

  # Real test rest-hook subscriber binary — built from
  # cmd/test-resthook-subscriber. Captures every notification request
  # the binary's rest-hook channel sends and exposes a query API at
  # /notifications for the test process. No in-process channel fake.
  test-resthook-subscriber:
    build:
      context: ../..
      dockerfile: e2e/realstack/fixtures/subscribers/Dockerfile.resthook
    image: fhir-subs-test-resthook-subscriber:dev
    ports:
      - "8090"
    healthcheck:
      test: ["CMD", "/usr/local/bin/test-resthook-subscriber-healthcheck"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  # Real test websocket subscriber binary — built from
  # cmd/test-ws-subscriber. Connects to the binary's WSS endpoint as a
  # real client, captures every event, exposes a query API at /events
  # for the test process. No in-process WS fake.
  test-ws-subscriber:
    build:
      context: ../..
      dockerfile: e2e/realstack/fixtures/subscribers/Dockerfile.ws
    image: fhir-subs-test-ws-subscriber:dev
    ports:
      - "8091"
    healthcheck:
      test: ["CMD", "/usr/local/bin/test-ws-subscriber-healthcheck"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  # OP #344: test-token-mint service deleted. Adversarial-auth claim-
  # override coverage moved to internal/api/auth/verifier_test.go (which
  # already covers expired, audience-mismatch, replayed-jti, unknown-
  # client, and revoked-client paths via in-process key generation).
  # Realstack tests that need a positive-path bearer token now use the
  # Keycloak client_credentials grant via Stack.MintClientToken.

  # Real test MLLP scripted control plane — built from
  # cmd/test-mllp-control-plane. Replaces the legacy
  # e2e/mockehr.ControlPlane in-process fake. Accepts POST /scenarios/*
  # JSON requests, synthesizes HL7 v2 frames (ADT/ORM/ORU), and emits
  # them over a real TCP socket to the prod binary's MLLP listener.
  # FHIRSUBS_MLLP_TARGET is set by the realstack harness once the prod
  # binary's MLLP port is allocated; the container can reach the host
  # via host.docker.internal (host-gateway alias for Linux runners).
  #
  # The "mllp" compose profile keeps this service out of the default
  # `up --wait` cycle so non-MLLP test runs do not pay the build cost.
  # The harness brings it up explicitly when Boot is called with
  # EnableMLLP=true.
  test-mllp-control-plane:
    profiles: ["mllp"]
    build:
      context: ../..
      dockerfile: e2e/realstack/fixtures/subscribers/Dockerfile.mllp_controlplane
    image: fhir-subs-test-mllp-control-plane:dev
    environment:
      FHIRSUBS_MLLP_TARGET: ${FHIRSUBS_MLLP_TARGET:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8093"
    healthcheck:
      test: ["CMD", "/usr/local/bin/test-mllp-control-plane-healthcheck"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

# OP #344: the otel-spans named volume was the file-exporter sink for
# the deleted otel-collector service; with both gone, this compose file
# no longer declares any named volumes.
