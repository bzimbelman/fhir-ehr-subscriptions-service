# Drop a corporate-MITM CA certificate at `cert.pem` (this directory) if you
# need to build the image behind a TLS-intercepting proxy (Netskope etc.).
# The file is gitignored. See hapi/Dockerfile for the import logic.
