# OP #155: keep generated demo key material out of version control.
# demo/scripts/generate-keys.sh writes at_rest_key here on first-time
# setup; demo/docker-compose.yml mounts it into the bridge container.
at_rest_key
*.key
*.pem
